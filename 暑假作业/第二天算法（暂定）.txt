#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
#include<stdlib.h>
#include<time.h>
#include<string.h>

char* Strchange(char str1[], char str2[],int length,char str3[])
{
	int i = 0,space=0;

	i = 0;
	while (i <= length + 2 * space)
	{
		if (str1[i] != ' ')
		{
			str3[i] = str1[i];
		}
		else
		{
			str3[i] = str2[0];
			i++;
			str3[i] = str2[1];
			i++;
			str3[i] = str2[2];
		}
		i++;
	}
	return str3;
}

int main()
{
	int i=0,length,leng,space=0;
	char str1[] = {'a','b','c',' ','d','e','f','g','x',' ','y','z'};
	char str2[] = {'%','2','0'};
	char *str3;
	length = sizeof(str1) / sizeof(str1[0]);
	
	while (i < length)
	{
		if (str1[i] == ' ')
		{
			space++;
		}
		i++;
	}
	i = 0;
	str3 = (char*)malloc((length + 2 * space)*sizeof(char));
	leng = length + 2 * space;
	char *str = Strchange(str1, str2, length, str3);
	while (i<leng)
	{
		printf("%c\n", *(str3+i));
	}
	system("pause");
	return 0;
}