# practice1

#include<stdio.h>
#include<stdlib.h>
#include<math.h>
int main()
{
	int a, i, m, suu=0,ss=0;
	scanf("%d", &a);
	for(i = 1; i <=5 ; i++)
	{
			ss += (pow(10, i - 1)*a);
		    suu +=ss;
	}
	printf("%d", suu);
	system("pause");
	return 0;
}


#include<stdio.h>
#include<stdlib.h>
#include<math.h>
int main()
{
	int i, x,y,z,p,sum;
	for (i = 100; i <= 999; i++)
	{
		p = i;
		x = p % 10;
		p = p / 10;
		y = p % 10;
		p = p / 10;
		z = p;
		if (pow(x, 3) + pow(y, 3) + pow(z, 3) == i)
		{
			printf("%5d", i);
		}
	}
	system("pause");
	return 0;
}
